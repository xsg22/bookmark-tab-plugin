# TabShelf - New Tab Bookmark Manager

## 一句话介绍

TabShelf 将 Chrome 新标签页变成一个面向效率场景的书签工作台，把书签、最近浏览、搜索和键盘导航集中到同一个页面里，帮助用户更快回到常用网站和刚访问过的页面。

## 核心功能概述

- 在新标签页中按分组展示书签，适合高频站点的集中访问
- 支持默认分组、分组排序和书签置顶，常用入口更容易被固定下来
- 内置最近浏览视图，可按最近访问或访问频次查看历史页面
- 支持全局搜索，统一检索书签、最近浏览，并快速发起 Google 搜索
- 支持键盘导航，可以在标签页、排序按钮和内容列表之间快速移动并打开目标页面
- 支持书签编辑、移动分组、备注维护和快速删除确认

## 适合的用户

- 书签很多、希望新标签页更像工作台的用户
- 需要在“固定入口”和“刚刚访问过的页面”之间快速切换的用户
- 偏爱键盘操作，希望减少鼠标点击次数的用户

## 功能亮点

### 1. 书签与最近浏览整合

TabShelf 不只展示静态书签，也把最近浏览放到同一个工作流里，方便用户在“长期沉淀的入口”和“短期上下文页面”之间快速切换。

### 2. 更适合高频使用的书签页

通过分组、置顶、默认分组和拖拽排序，用户可以把最常用的资源放在最顺手的位置，减少在层层目录里翻找的时间。

### 3. 键盘友好

支持在主标签、最近浏览排序选项和内容列表中使用方向键移动，并通过 Enter 或 Ctrl + Enter 快速打开页面，更适合高频操作场景。

### 4. 搜索即入口

同一个搜索框可以覆盖书签、最近浏览和搜索建议，让新标签页真正变成一个统一入口，而不是单纯的展示页面。

## 权限说明

- `bookmarks`：读取和管理书签分组、书签编辑、移动与置顶
- `history`：展示最近浏览记录，并支持按时间或频次排序
- `storage`：保存默认分组、布局偏好、备注和其他本地配置
- `favicon`：展示网页图标，提升识别效率

## Chrome 商店可用短描述

Keyboard-friendly new tab page for bookmarks, recent history, and fast access.

## Chrome 商店可用详细描述

TabShelf turns your new tab into a practical bookmark workspace.

Use it to organize bookmarks by folder, keep important links pinned, revisit recent pages, search across your workspace, and navigate quickly with the keyboard. It is designed for people who open Chrome many times a day and want a faster way to return to the pages they use most.

Key capabilities:

- Folder-based bookmark workspace
- Recent history view with multiple sorting modes
- Fast search across bookmarks and recent pages
- Keyboard navigation for tabs, sorting, and items
- Bookmark editing, notes, moving, pinning, and quick delete confirmation

TabShelf is ideal for productivity workflows, operations dashboards, development resources, knowledge bases, and any browsing routine that depends on fast repeated access.
